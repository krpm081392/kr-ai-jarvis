export default async function handler(req,res){const checks={};checks.env={gemini:Boolean(process.env.GEMINI_API_KEY),supabase:Boolean(process.env.SUPABASE_URL&&process.env.SUPABASE_SERVICE_ROLE_KEY),join:Boolean(process.env.JOIN_API_KEY&&process.env.JOIN_DEVICE_ID),googleSearch:Boolean(process.env.GOOGLE_SEARCH_API_KEY&&process.env.GOOGLE_SEARCH_CX),solana:Boolean(process.env.SOLANA_RPC_URL||process.env.SOLANA_WATCH_WALLET)};const base=getBaseUrl();for(const e of ["/api/health","/api/market-data?pair=BTC/USDT","/api/dashboard"]){checks[e]=await ping(base+e);}return res.status(200).json({ok:true,checks,uptime:"serverless",time:new Date().toISOString()});}async function ping(url){try{const r=await fetch(url);return {ok:r.ok,status:r.status};}catch(e){return {ok:false,error:e.message};}}function getBaseUrl(){if(process.env.VERCEL_URL)return`https://${process.env.VERCEL_URL}`;if(process.env.NEXT_PUBLIC_SITE_URL)return process.env.NEXT_PUBLIC_SITE_URL;return"http://localhost:3000";}