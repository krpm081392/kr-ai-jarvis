import { createClient } from '@supabase/supabase-js';
export default async function handler(req,res){try{const config={wakeWord:"Hey K-R",continuousMode:true,stopWords:["K-R stop listening","stop continuous mode"],tasker:"AutoVoice Recognized -> open KR app -> tap mic -> send voice",browser:"SpeechRecognition continuous mode where supported",ttsModes:["jarvis","bro","war"]};const s=getSupabase();if(s)await s.from("kr_logs").insert({type:"voice_mastery_check",data:config}).catch(()=>{});return res.status(200).json({ok:true,config});}catch(e){return res.status(500).json({ok:false,error:e.message});}}function getSupabase(){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return null;return createClient(url,key);}