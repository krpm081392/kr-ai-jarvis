export default async function handler(req,res){try{if(req.method!=="POST")return res.status(405).json({ok:false,error:"POST only"});const {topic=""}=req.body||{};const prompt=`K-R Teaching Engine. Teach Dikong this topic clearly:\n${topic}\n\nUse simple explanation, analogy, steps, example, common mistakes, and practice task. Adapt to beginner unless asked otherwise.`;const lesson=await askGemini(prompt);return res.status(200).json({ok:true,topic,lesson});}catch(e){return res.status(500).json({ok:false,error:e.message});}}async function askGemini(prompt){const key=process.env.GEMINI_API_KEY,model=process.env.GEMINI_MODEL||"gemini-1.5-flash";if(!key)return"Gemini key missing.";const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${key}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts:[{text:prompt}]}],generationConfig:{temperature:.45,maxOutputTokens:1600}})});const d=await r.json();return d?.candidates?.[0]?.content?.parts?.map(p=>p.text||"").join("\n").trim()||"No lesson.";}