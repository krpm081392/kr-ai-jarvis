import { createClient } from '@supabase/supabase-js';
export default async function handler(req,res){try{const s=getSupabase();if(!s)return res.status(200).json({ok:true,notifications:[]});if(req.method==="POST"){const {title="K-R",message="",type="manual"}=req.body||{};await s.from("kr_notifications").insert({title,message,type});return res.status(200).json({ok:true});}const {data=[]}=await s.from("kr_notifications").select("*").order("created_at",{ascending:false}).limit(100);return res.status(200).json({ok:true,notifications:data});}catch(e){return res.status(500).json({ok:false,error:e.message});}}function getSupabase(){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return null;return createClient(url,key);}