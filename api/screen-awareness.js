import { createClient } from '@supabase/supabase-js';
export default async function handler(req,res){try{if(req.method!=="POST")return res.status(405).json({ok:false,error:"POST only"});const {imageBase64="",prompt="What is on my screen?"}=req.body||{};if(!imageBase64)return res.status(400).json({ok:false,error:"imageBase64 required"});const r=await fetch(`${base()}/api/vision-analyze`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({imageBase64,prompt})});const d=await r.json();const analysis=d.analysis||d.error||"No analysis.";const s=getSupabase();if(s)await s.from("kr_screen_awareness").insert({analysis,prompt}).catch(()=>{});return res.status(200).json({ok:true,analysis});}catch(e){return res.status(500).json({ok:false,error:e.message});}}function base(){if(process.env.VERCEL_URL)return`https://${process.env.VERCEL_URL}`;if(process.env.NEXT_PUBLIC_SITE_URL)return process.env.NEXT_PUBLIC_SITE_URL;return"http://localhost:3000";}function getSupabase(){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return null;return createClient(url,key);}