export default async function handler(req,res){try{if(req.method!=="POST")return res.status(405).json({ok:false,error:"POST only"});const {goal=""}=req.body||{};if(!goal)return res.status(400).json({ok:false,error:"goal required"});const apiKey=process.env.GEMINI_API_KEY,model=process.env.GEMINI_MODEL||"gemini-1.5-flash";if(!apiKey)return res.status(200).json({ok:false,plan:"GEMINI_API_KEY missing."});const prompt=`K-R Code Agent: Dikong wants this KR upgrade: ${goal}\nCreate a safe code patch plan only. Include summary, files touched, API routes, env vars, safety risks, implementation steps. Do NOT claim you changed code.`;const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${apiKey}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts:[{text:prompt}]}],generationConfig:{temperature:.35,maxOutputTokens:1200}})});const d=await r.json();if(!r.ok)return res.status(500).json({ok:false,error:d?.error?.message||"Gemini error"});const plan=d?.candidates?.[0]?.content?.parts?.map(p=>p.text||"").join("\n").trim()||"No plan.";return res.status(200).json({ok:true,plan});}catch(e){return res.status(500).json({ok:false,error:e.message});}}