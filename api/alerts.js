import { createClient } from '@supabase/supabase-js';
export default async function handler(req,res){try{const s=getSupabase();if(!s)return res.status(200).json({ok:false,error:"Supabase env missing"});if(req.method==="GET"){const {data}=await s.from("kr_alerts").select("*").eq("active",true).order("created_at",{ascending:false}).limit(20);return res.status(200).json({ok:true,alerts:data||[]});}if(req.method==="POST"){const {type="manual",message="",priority="medium"}=req.body||{};await s.from("kr_alerts").insert({type,message,priority,active:true});return res.status(200).json({ok:true});}return res.status(405).json({ok:false,error:"Method not allowed"});}catch(e){return res.status(500).json({ok:false,error:e.message});}}function getSupabase(){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return null;return createClient(url,key);}