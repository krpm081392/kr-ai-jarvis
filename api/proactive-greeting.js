export default async function handler(req,res){try{const market=await safeFetch(`${base()}/api/market-data?pair=BTC/USDT&interval=15m`);const alerts=await safeFetch(`${base()}/api/alerts`);let greeting="Online, Dikong.";if(market?.ok){greeting=`Online, Dikong. BTC is ${market.indicators?.trend} on 15m, RSI ${market.indicators?.rsi14}.`;if(alerts?.alerts?.length)greeting+=` You have ${alerts.alerts.length} active alert(s).`;}return res.status(200).json({ok:true,greeting,market,alerts});}catch(e){return res.status(200).json({ok:true,greeting:"Online, Dikong. Live greeting fallback active."});}}async function safeFetch(url){try{const r=await fetch(url);return await r.json();}catch(e){return null;}}function base(){if(process.env.VERCEL_URL)return`https://${process.env.VERCEL_URL}`;if(process.env.NEXT_PUBLIC_SITE_URL)return process.env.NEXT_PUBLIC_SITE_URL;return"http://localhost:3000";}